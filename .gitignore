node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example
server/data
# Los adjuntos que se mandan desde el composer. Son datos de cada instalación,
# no del repo.
server/uploads

# App de escritorio: el build del renderer, los instaladores y el archivo de
# variables que arma el build a partir de server/.env + desktop/.env.
desktop/renderer
desktop/release
desktop/.env.production.local
