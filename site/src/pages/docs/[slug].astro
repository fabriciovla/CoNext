---
import Articulo from '../../views/Articulo.astro'
import { slugsDocs } from '../../data/docs'

export function getStaticPaths() {
  return slugsDocs().map((slug) => ({ params: { slug } }))
}
---

<Articulo seccion="docs" />
