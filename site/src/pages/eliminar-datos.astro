---
import Base from '../layouts/Base.astro'

// Esta URL se carga en la consola de Meta como "Eliminación de datos", así que
// no se cambia una vez publicada. El nombre del archivo es la ruta.
---

<Base
  title="Eliminación de datos — conext"
  description="Cómo pedir que se borren tus datos y los de tus conversaciones en conext."
>
  {/* El ancho de lectura lo pone la página: el layout dejó de imponer uno para
      que la landing pueda ir de borde a borde. El texto va acá adentro. */}
  <div class="mx-auto w-full max-w-3xl px-6 py-20"></div>
</Base>
