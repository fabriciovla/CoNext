---
import Base from '../layouts/Base.astro'

// Esta URL se carga en la consola de Meta, así que no se cambia una vez
// publicada. El nombre del archivo es la ruta.
---

<Base
  title="Términos y condiciones — chrm."
  description="Las condiciones de uso del servicio chrm."
>
  {/* El ancho de lectura lo pone la página: el layout dejó de imponer uno para
      que la landing pueda ir de borde a borde. El texto va acá adentro. */}
  <div class="mx-auto w-full max-w-3xl px-6 py-20"></div>
</Base>
